# Configs

Repository-wide safe config templates belong here. PC browser runtime config templates live under
`apps/sdkwork-models-pc/config/browser/`.

Catalog loading uses `SDKWORK_MODELS_CATALOG_ROOT` rather than application secrets or token config.
