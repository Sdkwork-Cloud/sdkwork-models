/** Admin ai model region price schema exposed by Claw Router. */
export interface AdminAiModelRegionPrice {
  /** Cache read price field on admin ai model region price. */
  cacheReadPrice: string;
  /** Cache write price field on admin ai model region price. */
  cacheWritePrice: string;
  /** Currency field on admin ai model region price. */
  currency: string;
  /** Price in field on admin ai model region price. */
  priceIn: string;
  /** Price out field on admin ai model region price. */
  priceOut: string;
  /** Region code field on admin ai model region price. */
  regionCode: string;
}
