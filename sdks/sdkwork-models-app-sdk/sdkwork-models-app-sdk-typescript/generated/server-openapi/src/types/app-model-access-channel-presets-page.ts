import type { AppModelAccessChannelPreset } from './app-model-access-channel-preset';

/** Official provider presets and their current public model lists. */
export interface AppModelAccessChannelPresetsPage {
  items: AppModelAccessChannelPreset[];
}
