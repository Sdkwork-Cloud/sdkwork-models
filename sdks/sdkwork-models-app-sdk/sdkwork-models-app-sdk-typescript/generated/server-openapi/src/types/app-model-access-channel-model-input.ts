/** One model selected for a model access channel. Capability metadata is derived from sdkwork-models. */
export interface AppModelAccessChannelModelInput {
  modelId: string;
  displayName?: string | null;
}
