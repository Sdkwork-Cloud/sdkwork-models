# Deployments

`sdkwork-models` ships immutable catalog JSON releases and the PC browser bundle from
`apps/sdkwork-models-pc/`.

Deployment descriptors and environment topology documentation belong here when GitHub packaging or
nginx publication is enabled for this repository.
