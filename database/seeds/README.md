# Catalog seeds

Catalog dictionary seed bundles are imported through host bootstrap (`sdkwork-models-database-bootstrap`) and Claw Router installer flows. Locale-specific seed payloads will live under `seeds/{locale}/` when boot-time seeding is enabled.
