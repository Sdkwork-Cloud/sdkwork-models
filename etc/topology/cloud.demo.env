# cloud.demo — cloud-hosted independent demonstration deployment, fully isolated from dev/test/staging/prod.
# Provisioned as its own deployment: dedicated PostgreSQL database/schema/user `sdkwork_models_demo`
# and a dedicated Redis logical DB index / `models-demo` key prefix so that no cache key
# (rate-limit, session, idempotency) ever collides with another shared-redis environment.
SDKWORK_MODELS_DEPLOYMENT_PROFILE=cloud
SDKWORK_MODELS_ENVIRONMENT=demo
SDKWORK_ENV=demo
SDKWORK_MODELS_PROFILE_ID=cloud.demo


# Region deployment dimension (REGION_SPEC.md): orthogonal to deployment profile and environment.
SDKWORK_MODELS_REGION_CODE=cn
SDKWORK_DATABASE_SEED_LOCALE=zh-CN

# Dedicated demo public hosts (see topology.spec.json cloudPublicHosts environments["demo"]).
SDKWORK_MODELS_APPLICATION_PUBLIC_HTTP_URL=https://models-demo.sdkwork.com
SDKWORK_MODELS_PLATFORM_API_GATEWAY_HTTP_URL=https://api-demo.sdkwork.com

VITE_SDKWORK_MODELS_DEPLOYMENT_PROFILE=cloud
VITE_SDKWORK_MODELS_APPLICATION_PUBLIC_HTTP_URL=https://models-demo.sdkwork.com
VITE_SDKWORK_MODELS_PLATFORM_API_GATEWAY_HTTP_URL=https://api-demo.sdkwork.com

# Redis isolation: provisioning must assign a dedicated logical DB index / `models-demo` prefix
# on the shared Redis so cache, rate-limit, and idempotency keys stay confined to this environment.