# Crates

Rust route crates, service crates, and API servers for `sdkwork-models` belong here when HTTP or RPC
runtime code is added to this repository.

Catalog loading for Rust consumers currently lives in
`sdks/sdkwork-models-sdk/sdkwork-models-sdk-rust/`.
